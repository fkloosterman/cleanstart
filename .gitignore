# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
.env
# Per-environment secrets (.env.dev, .env.prod, …) — never commit; the
# tracked template is .env.example.
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Vercel
.vercel

# Supabase CLI local cache
supabase/.temp
supabase/.branches/*
supabase/snippets/*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code local overrides (personal, not shared with the team)
.claude/settings.local.json
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
